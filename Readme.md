# Migrate deprecated function.property to Ember.compute

Run:
* `npm install`
* `node index.js -d [source directory] -o [output directory]`

Optionally you can pass `--dry-run` to safely check changes without really writing (or overwriting) files.
Some author notes are present at the end of the file.